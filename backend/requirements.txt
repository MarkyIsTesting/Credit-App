# --- Runtime dependencies (production) ---
fastapi==0.110.1
uvicorn==0.25.0
# watchfiles powers uvicorn --reload; cross-platform (Windows + Linux), unlike uvloop.
watchfiles>=0.21.0
python-dotenv>=1.0.1

# Database
motor==3.3.1
pymongo==4.6.3

# Validation / models
pydantic>=2.6.4
email-validator>=2.2.0

# Auth
pyjwt>=2.10.1
bcrypt==4.1.3

# Email (standard SMTP, provider-agnostic)
aiosmtplib>=3.0.1

# Timezone data (needed on slim/Windows images)
tzdata>=2024.2
